# Управление счетами и финансовая аналитика

Центральный модуль управления финансовыми счетами в приложении "Hasta la Vista, Money!" с встроенной аналитикой доходов и расходов.

## Обзор функциональности

### Основные возможности

- **Управление счетами** - создание, редактирование и удаление финансовых счетов
- **Переводы между счетами** - переводы средств с логированием операций
- **Финансовая аналитика** - графики доходов и расходов в реальном времени
- **Мультивалютность** - поддержка 8 валют (RUB, USD, EUR, GBP, CZK, PLN, TRY, CNH)
- **Типы счетов** - кредитные, дебетовые счета и наличные
- **Журнал операций** - полная история переводов между счетами

### Структура данных

Каждый счет содержит следующую информацию:

- **Название** - уникальное имя счета (до 250 символов)
- **Тип** - кредитный, дебетовый или наличные
- **Баланс** - текущая сумма на счете (до 20 знаков)
- **Валюта** - валюта счета из поддерживаемого списка
- **Пользователь** - владелец счета
- **Дата создания** - автоматически заполняется

## Интерфейс

### Главная страница счетов

![accounts.png](https://media.githubusercontent.com/media/TurtleOld/hasta-la-vista-money/refs/heads/main/docs/img/accounts.png)

Главная страница состоит из четырех основных секций:

#### 1. График доходов и расходов

!!! info "Аналитика доходов и расходов"

    Интерактивный график показывает динамику ваших финансов:
    
    - **Зеленая линия** - доходы по дням
    - **Красная линия** - расходы по дням
    - **Временная шкала** - автоматически объединяет данные по датам
    - **Пустой график** - если данных еще нет
    
    График автоматически обновляется при добавлении новых доходов или расходов.

#### 2. Общий баланс всех счетов

!!! info "Суммарный баланс"

    Отображает общую сумму средств по всем вашим счетам:
    
    - **Автоматический пересчет** при изменениях балансов
    - **"Нет данных для отображения"** - если счетов еще нет
    - **Мультивалютность** - позволяет указывать разные валюты на счетах

#### 3. Таблица управления счетами

!!! info "Управление счетами"

    Основная таблица для работы со счетами:
    
    - **Название счета** - отображается в алфавитном порядке
    - **Текущий баланс** - актуальная сумма на счете
    - **Валюта** - символ валюты счета
    - **Кнопки действий** - создание (+) и переводы (⇄)
    - **"Счета еще не созданы!"** - если счетов нет

#### 4. Статистика пользователя

!!! info "Детальная статистика"

    Кнопка перехода к подробной аналитике:
    
    - **Анализ расходов** - по категориям и периодам
    - **Анализ доходов** - источники поступлений

## Управление счетами

### Создание нового счета

!!! abstract "Пошаговая инструкция"

    1. **Нажмите кнопку "+"** в таблице счетов
    2. **Заполните форму:**
       - Название счета (обязательно)
       - Начальный баланс (обязательно)
       - Валюта (обязательно)
       - Тип счета (по умолчанию дебетовый)
    3. **Нажмите "Сохранить"**

#### Типы счетов

| Тип | Описание | Использование |
|-----|----------|---------------|
| **Кредитный** | Счет с возможностью отрицательного баланса | Кредитные карты, овердрафт |
| **Дебетовый** | Счет с положительным балансом | Основные банковские счета |
| **Наличные** | Физические деньги | Наличные средства |

#### Поддерживаемые валюты

- 🇷🇺 **RUB** - Российский рубль
- 🇺🇸 **USD** - Доллар США  
- 🇪🇺 **EUR** - Евро
- 🇬🇧 **GBP** - Британский фунт
- 🇨🇿 **CZK** - Чешская крона
- 🇵🇱 **PLN** - Польский злотый
- 🇹🇷 **TRY** - Турецкая лира
- 🇨🇳 **CNH** - Китайский юань

### Переводы между счетами

!!! abstract "Процедура перевода"

    1. **Нажмите кнопку "⇄"** в таблице счетов
    2. **Выберите счета:**
       - Счет списания (откуда переводим)
       - Счет зачисления (куда переводим)
    3. **Введите сумму** перевода
    4. **Укажите дату** операции (по умолчанию текущая)
    5. **Добавьте заметку** (опционально)
    6. **Нажмите "Выполнить перевод"**

!!! tip "Важные ограничения"

    - **Нельзя переводить** между одинаковыми счетами
    - **Недостаточно средств** - перевод не выполнится
    - **Автоматическое логирование** - все переводы записываются в журнал

#### Валидация переводов

Система проверяет:

- **Достаточность средств** на счете списания
- **Различность счетов** (нельзя переводить на тот же счет)
- **Корректность суммы** (положительное число)
- **Принадлежность счетов** пользователю

### Редактирование счета

!!! abstract "Изменение данных счета"

    1. **Найдите счет** в таблице
    2. **Нажмите кнопку "✏️"** рядом с нужным счетом
    3. **Измените данные:**
       - Название счета
       - Тип счета
       - Баланс
       - Валюту
    4. **Сохраните изменения**

!!! warning "Внимание при изменении баланса"

    При изменении баланса счета система не создает запись о переводе.
    Используйте эту функцию для корректировки данных, а не для учета операций.

### Удаление счета

!!! abstract "Удаление счета"

    1. **Найдите счет** в таблице
    2. **Нажмите кнопку "🗑️"** рядом с нужным счетом
    3. **Подтвердите удаление**

!!! danger "Ограничения удаления"

    - **Счет с балансом** - удаление заблокировано
    - **Связанные операции** - проверьте историю переводов
    - **Необратимость** - удаленные счета нельзя восстановить

## Финансовая аналитика

### График доходов и расходов

График автоматически собирает данные из всех модулей:

#### Источники данных

- **Расходы** - из модуля `expense`
- **Доходы** - из модуля `income`
- **Чеки** - автоматически конвертируются в расходы
- **Переводы** - не влияют на график (внутренние операции)

#### Обработка данных

```python
# Автоматическое объединение по датам
expense_dataset = Expense.objects.filter(user=user).values('date').annotate(
    total_amount=Sum('amount')
).order_by('date')

income_dataset = Income.objects.filter(user=user).values('date').annotate(
    total_amount=Sum('amount')
).order_by('date')
```

#### Особенности отображения

- **Уникальные даты** - дублирующиеся даты объединяются
- **Сортировка** - данные отображаются в хронологическом порядке
- **Масштабирование** - автоматическое подстраивание под данные
- **Интерактивность** - наведение показывает точные значения

### Детальная статистика

!!! abstract "Доступ к статистике"

    1. **Нажмите кнопку** "Общая статистика пользователя <имя>"
    2. **Перейдите** на страницу подробной аналитики
    3. **Изучите** различные отчеты и графики

#### Доступные отчеты

- **Расходы по категориям** - структура трат
- **Доходы по источникам** - анализ поступлений
- **Месячная динамика** - тренды изменения
- **Сравнительная аналитика** - периоды vs периоды

## Журнал переводов

### Структура записи

Каждый перевод логируется с полной информацией:

```python
class TransferMoneyLog(models.Model):
    user = models.ForeignKey(User, on_delete=models.CASCADE)
    from_account = models.ForeignKey(Account, related_name='from_account')
    to_account = models.ForeignKey(Account, related_name='to_account')
    amount = models.DecimalField(max_digits=20, decimal_places=2)
    exchange_date = models.DateTimeField()
    notes = models.TextField(blank=True)
    created_at = models.DateTimeField(auto_now_add=True)
```

### Просмотр истории

- **Хронологический порядок** - новые переводы сверху
- **Детальная информация** - счета, суммы, даты, заметки
- **Фильтрация** - по счетам, периодам, суммам
- **Экспорт** - возможность выгрузки данных

### Пример записи

```text
20-12-2023 15:30. Перевод суммы 5000.00 со счёта "Основной счёт" на счёт "Сбережения".
```

## API и интеграции

### REST API эндпоинты

```http
GET    /api/accounts/              # Список счетов
POST   /api/accounts/              # Создание счета
GET    /api/accounts/{id}/         # Получение счета
PUT    /api/accounts/{id}/         # Обновление счета
DELETE /api/accounts/{id}/         # Удаление счета
POST   /api/transfers/             # Создание перевода
GET    /api/transfers/             # История переводов
```

### Модели данных

#### Account (Счет)

```json
{
  "id": 1,
  "user": 1,
  "name_account": "Основной счет",
  "type_account": "D",
  "balance": "15000.00",
  "currency": "RUB",
  "created_at": "2023-12-20T15:30:00Z"
}
```

#### TransferMoneyLog (Перевод)

```json
{
  "id": 1,
  "user": 1,
  "from_account": 1,
  "to_account": 2,
  "amount": "5000.00",
  "exchange_date": "2023-12-20T15:30:00Z",
  "notes": "Перевод на сбережения",
  "created_at": "2023-12-20T15:30:00Z"
}
```

## Безопасность и валидация

### Права доступа

- **Аутентификация** - все операции требуют входа
- **Изоляция данных** - пользователи видят только свои счета
- **Проверка владельца** - операции только с собственными счетами

### Валидация данных

```python
# Проверка достаточности средств
if amount > from_account.balance:
    raise ValidationError("Недостаточно средств на счете")

# Проверка различных счетов
if from_account == to_account:
    raise ValidationError("Нельзя переводить на тот же счет")
```

### Защита от ошибок

- **Транзакционность** - переводы выполняются атомарно
- **Логирование** - все операции записываются
- **Восстановление** - возможность отката при ошибках

## Производительность

### Оптимизация запросов

```python
# Использование select_related для уменьшения запросов
accounts = Account.objects.select_related('user').filter(user=request.user)

# Аннотации для агрегации
total_balance = Account.objects.filter(user=user).aggregate(
    total=Sum('balance')
)
```

### Кэширование

- **Графики** - кэширование аналитических данных
- **Балансы** - кэширование общих сумм
- **Списки счетов** - кэширование для быстрого доступа

## Устранение неполадок

### Частые проблемы

#### "Недостаточно средств на счете"

**Причина:** Попытка перевода суммы, превышающей баланс.

**Решение:**
- Проверьте текущий баланс счета
- Уменьшите сумму перевода
- Пополните счет перед переводом

#### "Нельзя переводить на тот же счет"

**Причина:** Выбраны одинаковые счета для перевода.

**Решение:**
- Выберите разные счета
- Проверьте правильность выбора

#### "Счет не найден"

**Причина:** Счет был удален или недоступен.

**Решение:**
- Проверьте список доступных счетов
- Создайте новый счет при необходимости
- Обратитесь к администратору

### Логи и отладка

```python
# Логирование операций
import logging
logger = logging.getLogger(__name__)

def transfer_money(from_account, to_account, amount):
    try:
        success = from_account.transfer_money(to_account, amount)
        if success:
            logger.info(f"Transfer successful: {amount} from {from_account} to {to_account}")
        else:
            logger.warning(f"Transfer failed: insufficient funds")
        return success
    except Exception as e:
        logger.error(f"Transfer error: {e}")
        raise
```

## Настройки и конфигурация

### Пользовательские настройки

- **Валюта по умолчанию** - RUB (настраивается)
- **Тип счета по умолчанию** - Дебетовый
- **Формат отображения** - настраиваемые шаблоны

### Системные ограничения

- **Максимальная длина названия** - 250 символов
- **Точность баланса** - 2 знака после запятой
- **Максимальная сумма** - 999,999,999,999,999,999.99
- **Количество счетов** - не ограничено

## Планы развития

### Будущие функции

- **Конвертация валют** - автоматические курсы обмена
- **Планировщик переводов** - регулярные операции
- **Уведомления** - о низком балансе, переводах
- **Экспорт данных** - CSV, Excel, PDF отчеты
- **Мобильное приложение** - нативная поддержка

---

**Hasta la Vista, Money!** - Центр управления вашими финансами! 💰